enum UserType {
  customer,
  chef,
  selection, // Muestra la pantalla de selección original
}

// Cambia esta constante para elegir el tipo de usuario inicial
const UserType currentUserRole = UserType.customer;
